# Local Demo Deployment

This project is configured for local demo use.

```bash
npm install
npm run build
npm run dev
```

For a production deployment, add a real persistence layer and replace the local demo session/data modules.
