# Local Demo Data

The localhost version does not use a database.

Demo users, student records, attendance, marks, notices, fees, timetable entries, audit logs, and ReClaim items live in:

```txt
src/lib/demo-data.ts
```

Update that file to change the data shown in the app.
