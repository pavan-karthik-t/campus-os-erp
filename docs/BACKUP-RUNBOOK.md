# Local Demo Backup Runbook

CampusOS currently uses local fixture data in `src/lib/demo-data.ts`.

There is no external database or storage service to back up for the localhost demo. To preserve demo changes, commit the project files to version control.
